# 2026-09-22 ACME P0-1 本地与一次性 VM 验证

本记录只证明已执行的范围；`AcmeConfig/` **尚未通过真实 CA 签发和续期验收，不能用于生产**。
未使用真实 DNS Token、生产域名或生产主机。当前分支为 `codex/devopstoolkit-hardening`；
测试开始时基线 HEAD 为 `74ef67b11b760a63d53f4f8f975d5e19fcf07405`。
本次代码在验证时仍未提交，提交后的精确 SHA 须另行补录，不能将基线 SHA 冒充候选实现 SHA。

## 本地质量门禁

- `./tests/verify-ansible.sh`：通过，包含 Ansible syntax、向导、安装器、Release 与 13 项 ACME 单测。
- `shellcheck AcmeConfig/*.sh AcmeConfig/bin/* AcmeConfig/tests/vm-smoke.sh tests/verify-ansible.sh`：通过。
- `ruff check AcmeConfig/libexec/acme-manager AcmeConfig/tests/test_acme_manager.py`（0.9.10）：通过。
- `actionlint`、`gitleaks detect --source . --redact --no-banner`、`yamllint .`：均退出 0。
  `yamllint` 对旧 Ansible/Release 文件有 3 条既有行长警告，非本次新增错误。
- `git diff --check`：通过。

## VM 与供应链边界

- 一次性实例 `devopstoolkit-acme-p01-20260922`，Ubuntu 24.04 LTS；仅用于本次测试。
- VM 无法连接 `codeload.github.com:443`：联网安装以 curl exit 28 失败，**未绕过**哈希门槛。
- 宿主机预先校验的 acme.sh 3.1.6 固定提交
  `807da6498377ee5e0cf43a78091f46f12dc59a89` 归档 SHA256 为
  `ddbe1bcbd1a44a2623a2af167ebdc678669e6e2eb396742f2d1d28e02dc14220`。
  传入 `--archive` 后，VM 再次校验 SHA256 成功；错误归档则在 `sha256sum` 处失败。
- 第一次安装暴露 acme.sh 安装器需要在源码目录执行的问题；修复后从失败留下的状态直接重跑成功。
- 第二次初始化复用固定程序；`/etc/acme/acme-source` 包含归档与已安装程序 SHA256，
  `AUTO_UPGRADE='0'`，健康检查为 0 fail。
- 恶意邮箱 `$(id)@example.com` 被白名单拒绝，未执行 shell 展开。

## 权限、部署与沙箱

- 健康检查确认 `acme` 为 nologin，仅在 `acme`/`acme-secrets`，不在 `ssl-cert`；
  `www-data` 在 `ssl-cert`，不在 `acme-secrets`。
- `/etc/acme/dns-config` 为 `root:acme-secrets 0640`；`acme` 可读，`www-data` 不可读。
- `/var/lib/acme/certs` 为 `root:ssl-cert 0750`，测试私钥为 `root:ssl-cert 0640`；
  `acme` 不可读已发布私钥，`www-data` 仅有读取权限。
- `systemd-analyze verify` 通过；`acme-renew.service` 在受限用户下运行成功。
  `systemd-analyze security` 的 renew/deploy 暴露评分均为 `2.9 OK`；这只是沙箱配置指标，
  不等于 ACME 真实签发成功。
- `AcmeConfig/tests/vm-smoke.sh` 使用临时自签证书：空队列无 reload；成功部署后仅活动测试
  service 收到 reload；staging 符号链接被拒绝，未发布、未 reload，marker 进入失败目录。
- 此用例发现 systemd `ReadWritePaths` 下跨 bind mount `rename` 返回 `EXDEV` 并导致重复触发；
  已改为在隔离目录新建 root 控制的失败记录，再移除请求，VM 重测通过。
- OpenSSL 校验已覆盖证书有效期、域名、证书链和私钥公钥匹配；13 项单测与 VM 自签证书部署通过。
  真实 CA 链兼容性尚未验证。

## 清理与备份

- `acme-cleanup.sh` 默认只打印固定路径清单；VM dry-run 后原状态保留，且未创建备份目录。
- `--yes` 后先生成 root-only `0600` 的归档，再停用 unit、移除托管路径；不删除账户/组。
- 归档在隔离目录成功解包，测试证书可被 OpenSSL 解析，DNS 配置与私钥恢复出的
  owner/mode 分别为 `root:acme-secrets 0640`、`root:ssl-cert 0640`。
- 测试归档和解包目录只存在于一次性 VM；它们含测试私钥，未复制进仓库。

## 未覆盖与下一门槛

1. 没有受控测试域名、DNS Token 和可访问的 ACME CA，故未执行真正的首次签发、
   DNS/webroot 挑战、真实续期及真实链验证；自签证书仅证明本地部署边界。
2. 尚未对真实 DNS provider 响应、日志内容与密钥持久化做脱敏审计。
3. 当前每个证书文件分别原子替换，证书 bundle 在进程中断时仍可能短暂出现新 key/旧 cert；
   上生产前应实现整组事务切换或可靠回滚，并注入故障验证。
4. 未执行 Ubuntu 22.04、其他 Web 服务账户或真实消费者配置的兼容性验收。
