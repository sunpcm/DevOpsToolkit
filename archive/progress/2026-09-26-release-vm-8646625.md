# 候选提交 8646625 的发布前一次性 VM 验收

日期：2026-09-26。候选提交：`86466259d3632d224e655b66be56e65240a252f0`。
验收时远端 `main` 与该提交相同；独立工作树为干净状态。这个报告只证明该
SHA 的发布前 VM 门槛，不代表已创建新 tag、批准或完成 Release。

## 原件与复核方法

机器可读原件见 [`2026-09-26-release-vm-8646625.report`](2026-09-26-release-vm-8646625.report)，
SHA256：`0ff921b20b504b5c32fc26c49853af2dceab842633c678466c900aefacf126df`。
原始运行日志在受控目录 `/private/tmp/devopstoolkit-p12v3.OV8K1o/vm-8646625-run.log`，
静态门禁日志为同目录的 `candidate-8646625-verify.log`；临时目录不是长期归档。
复核报告原件时，在仓库根目录运行：

```bash
shasum -a 256 archive/progress/2026-09-26-release-vm-8646625.report
python3 scripts/verify-vm-evidence.py verify-report \
  --report archive/progress/2026-09-26-release-vm-8646625.report \
  --sha 86466259d3632d224e655b66be56e65240a252f0
```

在该 SHA，`./tests/verify-ansible.sh` 退出码为 0；GitHub
[Validate run](https://github.com/sunpcm/DevOpsToolkit/actions/runs/36246649645)
状态为 `success`。隔离控制端使用现有 `ansible-core==2.21.4` 与已锁定 collections，
没有在 macOS 宿主机安装新依赖。一次性验收实际调用：

```bash
./tests/multipass-smoke.sh run --with-faults --with-uv \
  --report /private/tmp/devopstoolkit-p12v3.OV8K1o/vm-report-8646625.report
```

命令退出码为 0，报告校验脚本退出码为 0。报告记录
`source_dirty=false`、`ansible_core=2.21.4`、`test_uv=1`、`test_faults=1`、
`result=passed` 与 `cleanup_status=passed`。结束后独立执行
`multipass list --format json`，实例列表为空。

## 实测范围

- Ubuntu 22.04.5 LTS（aarch64）：首次配置、普通用户新 SSH 端口 finalize、
  第二次完整运行 `ok=73 changed=0 unreachable=0 failed=0`；
  SSH/UFW/Docker/Nginx 检查通过。
- Ubuntu 24.04.5 LTS（aarch64）：同样流程，第二次完整运行
  `ok=75 changed=0 unreachable=0 failed=0`；服务检查通过。
- 两版均覆盖错误私钥、未确认密钥、SSH/UFW 中断恢复、无效 sshd 配置、
  陈旧 UFW profile 与 Docker APT 源、被占用端口、部分账户状态后完整重跑，
  以及禁用 root 登录后普通用户仍能管理。固定 uv 归档也纳入测试。
- 独立只读复核结论为 `APPROVED`：核对报告 SHA、源码干净状态、实际系统版本、
  首次/二次运行、故障路径和最终清理状态。

本次不覆盖 amd64、真实 WSL2、macOS `sudo --system`、正式签名资产的安装/回滚，
也不覆盖暂缓的 ACME 真实 CA。人工审批必须重新核对报告原件、上述 SHA256、
tag 目标 SHA 和报告 8 天有效期；任何一项不符时不得批准 Release。本报告仅对
`86466259d3632d224e655b66be56e65240a252f0` 有效。合并本证据文档会产生新的
`main` SHA；若改用该新 SHA 发布，必须重新运行当次 VM 验收，不能沿用本报告。
