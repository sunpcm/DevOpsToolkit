# Managed by DevOpsToolkit SSH finalize guard
[{{ firewall_ssh_finalize_guard_name }}]
title=Temporary verified SSH finalize port guard
description=Kept until the managed firewall profile converges successfully
ports={{ ssh_port }}/tcp
